/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.shopify.com',
      'static.vecteezy.com',
      'images.pexels.com',
      'images.stockcake.com',
      'media.istockphoto.com'
    ]
  }
}

module.exports = nextConfig;
